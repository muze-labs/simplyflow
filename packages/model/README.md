# @muze-labs/simplyflow-model

Model helpers for sorting, filtering, paging, columns, and scrolling.

```javascript
import { model, sort, filter, paging, columns } from '@muze-labs/simplyflow-model'
```

This package depends on `@muze-labs/simplyflow-state`, is pure ESM, and is marked as side-effect-free for bundlers.
