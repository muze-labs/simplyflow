# @muze-labs/simplyflow-bind

DOM binding helpers extracted from SimplyFlow.

```javascript
import { bind } from '@muze-labs/simplyflow-bind'
```

This package depends on `@muze-labs/simplyflow-state`, is pure ESM, and is marked as side-effect-free for bundlers.
